Investigate the legacy v2_1 project and compare it against the current project.

Determine exactly which automated mapping components already existed in v2_1 (Part_1_Legacy_v2_1_AutoMapping_Architecture_Analysis.md and Part_2_Legacy_v2_1_Metadata_Discovery_Deep_Dive.md).

Specifically identify:

table matching algorithms
column matching algorithms
metadata extraction
similarity scoring
confidence scoring
discovery logic
reusable classes

For each component determine:

whether an equivalent already exists in the current project
whether it should be migrated
whether it should be redesigned
estimated implementation effort

Produce a document called:

Part_4_Legacy_Mapping_Migration_Strategy.md in 'Research' folder

Do not modify any source code.