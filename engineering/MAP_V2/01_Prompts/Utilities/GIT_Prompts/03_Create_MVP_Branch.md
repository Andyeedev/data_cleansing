Prompt 3 — Create the MVP Branch

After the baseline commit is complete:

Create a new branch from that commit:

feature/map-v2-mvp

Confirm:

baseline commit hash
tag
new branch
working tree clean

Do not modify any application code.