version: '3.9'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: engine
      POSTGRES_PASSWORD: engine
      POSTGRES_DB: migration_engine
    ports:
      - "5432:5432"
