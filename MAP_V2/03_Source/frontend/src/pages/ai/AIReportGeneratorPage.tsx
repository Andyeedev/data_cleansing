import { AIReportGenerator } from '../../ai/report-generator';

export const AIReportGeneratorPage = () => {
  return (
    <div style={{ height: '100%' }}>
      <AIReportGenerator />
    </div>
  );
};

export default AIReportGeneratorPage;
